# ⚠️ CPU-ONLY Build (NO GPU SUPPORT)
# WARNING: This will NOT work with quantum_unconscious or GPU acceleration!
# Use this ONLY for CPU-only deployments WITHOUT quantum computing
#
# For GPU + Quantum: Install requirements-core.txt + requirements_core_quantum.txt
# For Development: Install requirements-core.txt + requirements-dev.txt

# Web Framework
fastapi>=0.122.0
uvicorn>=0.38.0

# Core ML (CPU versions)
torch>=2.9.0  # CPU-only
transformers>=4.57.0

# Vector Database
qdrant-client>=1.16.0,<2.0.0

# Data Models & Config
pydantic>=2.12.0
python-dotenv>=1.2.0
PyYAML>=6.0.3

# Data Processing
numpy>=2.3.0
pandas>=2.2.0

# Testing
pytest>=9.0.0
pytest-cov>=7.0.0
pytest-asyncio>=1.3.0

# Code Quality
black>=24.0.0
flake8>=7.0.0
mypy>=1.8.0

# Database
redis>=7.0.0

# Utils
rich>=14.0.0
psutil>=7.0.0
requests>=2.32.0
