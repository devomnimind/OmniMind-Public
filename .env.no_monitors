# 🔇 TESTING MODE - Run with test limits (not aggressive killing)
# These processes will NOT be killed during development/testing
# Resource protector stays ACTIVE but in "test" mode (lenient)

# Resource protector MODE: "dev", "test", or "prod"
# "test" mode: CPU 85%, Memory 85%, Startup grace period 30s
OMNIMIND_RESOURCE_PROTECTOR_MODE=test

# Don't disable - just use lenient mode
# OMNIMIND_DISABLE_RESOURCE_PROTECTOR=0 (implied)

# Allow overcommit on system level
OMNIMIND_ALLOW_MEMORY_OVERCOMMIT=1

# Verbose logging to see what's happening
OMNIMIND_MONITOR_DEBUG=1

# Test process whitelist (PID will be registered for protection)
OMNIMIND_TEST_MODE=1
